// dependencies
import React from 'react'
import { Typography } from '@material-ui/core'

export default props => (
    <Typography
        variant="h4"
    >
    Create New Proposal
    </Typography>
)